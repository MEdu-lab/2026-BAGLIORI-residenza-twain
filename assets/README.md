# Assets

Metti qui i file statici come loghi e immagini.

## Logo

Per aggiungere un logo all'header del form:

1. Aggiungi il file `logo.png` (o `.jpg`, `.svg`) in questa cartella
2. Nel file `data.yaml` aggiungi:

```yaml
logo: "logo.png"
```

Il logo verrà centrato nell'header del documento.

## Formati supportati

- PNG (consigliato)
- JPG/JPEG
- SVG
- GIF
